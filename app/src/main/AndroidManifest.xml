<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.servenep">

    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ServeNep"
        android:usesCleartextTraffic="true">
        <service
            android:name=".notification.MyFirebasemessingServices"
            android:enabled="true"
            android:permission="com.google.android.c2dm.permission.SEND">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
            </intent-filter></service>

        <activity android:name=".UI.TaskerRecyclerViewActivity"></activity>
        <activity
            android:name=".UI.RegisterActivity"
            tools:ignore="IntentFilterExportedReceiver"></activity>
        <activity android:name=".UI.TaskerProfileActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.TaskerBookingActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".UI.JobNotificationActivity"
            tools:ignore="WrongManifestParent" />
        <activity
            android:name=".UI.DashboardActivity"
            tools:ignore="WrongManifestParent" />
        <activity
            android:name=".Adapter.TaskerAdapter"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".UI.TaskDescriptionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UI.LoginActivity"
            tools:ignore="WrongManifestParent" />
        <activity
            android:name=".UI.MainActivity"
            tools:ignore="WrongManifestParent" />
    </application>

</manifest>